data:
  split: person
  train: 8
  val: 1
  test: 1

preprocess:
  lowpass: true
  isolation_forest:
    contamination: 0.05
  normalize: zscore

model:
  type: cnn
  input_shape: [333, 1]

training:
  loss: mse
  metric: mae
  epochs: 100
  batch_size: 32
  learning_rate: 0.001
